# 网站部署验证 - 最终检查

## ✅ Nginx 配置已重载

- ✅ Nginx 配置语法正确
- ✅ 配置已重载（警告可忽略）

## 🔍 继续验证

### 第一步：检查后端服务状态

```bash
pm2 status
```

应该看到 `feihub-backend` 在运行。

### 第二步：检查后端日志（确认服务正常）

```bash
pm2 logs feihub-backend --lines 10 --nostream
```

### 第三步：测试 API 端点

```bash
# 测试健康检查接口
curl http://localhost:4000/api/health

# 应该返回 JSON，类似：
# {"status":"ok","timestamp":"..."}
```

### 第四步：检查前端静态文件

```bash
# 检查 dist 文件夹内容
ls -la /www/wwwroot/feihub/dist/assets/

# 应该看到 CSS 和 JS 文件
```

### 第五步：测试网站访问

1. **访问网站**：
   - http://feihub.top
   - http://121.40.214.130

2. **检查浏览器控制台**（按 F12）：
   - Console 标签：查看是否有错误
   - Network 标签：查看 API 请求是否成功

3. **测试功能**：
   - ✅ 页面是否正常显示
   - ✅ 搜索功能
   - ✅ 提交文档功能
   - ✅ 文档列表显示

## 🐛 如果遇到问题

### 问题1：页面显示空白

```bash
# 检查文件权限
chmod -R 755 /www/wwwroot/feihub/dist
chown -R www:www /www/wwwroot/feihub/dist

# 检查 Nginx 错误日志
tail -20 /www/wwwlogs/feihub.top.error.log
```

### 问题2：API 请求失败（Failed to fetch）

```bash
# 检查后端服务
pm2 status
pm2 logs feihub-backend --lines 20

# 检查后端端口
netstat -tlnp | grep 4000

# 检查后端是否响应
curl http://localhost:4000/api/health
```

### 问题3：静态资源 404

```bash
# 检查 assets 文件夹
ls -la /www/wwwroot/feihub/dist/assets/

# 检查 Nginx 配置中的 root 路径
grep "root" /www/server/panel/vhost/nginx/feihub.top.conf
```

## 🎯 快速验证命令

```bash
# 1. 检查后端
pm2 status
curl http://localhost:4000/api/health

# 2. 检查前端文件
ls -la /www/wwwroot/feihub/dist/assets/

# 3. 检查 Nginx 日志
tail -10 /www/wwwlogs/feihub.top.error.log
```

---

请先执行第一步和第三步（`pm2 status` 和 `curl http://localhost:4000/api/health`），把结果发给我。


# 手动删除重复代码

## 🔧 在服务器上执行

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub/backend/src/lib

# 备份
cp feishu-puppeteer.ts feishu-puppeteer.ts.backup10

# 使用 sed 删除重复的行
# 删除第383行的空行和第384行的重复日志
sed -i '383d' feishu-puppeteer.ts  # 删除空行
sed -i '383d' feishu-puppeteer.ts  # 删除重复的 "准备解析日期"

# 删除第388行的重复日志（"解析后的日期"）
sed -i '388d' feishu-puppeteer.ts

# 验证修复
echo "修复后的代码："
sed -n '360,390p' feishu-puppeteer.ts
```

---

## ✅ 然后重新构建

```bash
cd /www/wwwroot/feihub/backend

npm run build

if [ $? -eq 0 ]; then
    echo "✅ 构建成功"
    pm2 restart feihub-backend
else
    echo "❌ 构建失败"
    npm run build 2>&1 | tail -30
fi
```


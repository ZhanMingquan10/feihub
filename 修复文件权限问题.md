# 修复文件权限问题

## 🔍 问题说明

错误：`Operation not permitted`

这是因为文件权限问题，需要使用 `sudo` 或修改文件权限。

---

## 🔧 解决方案

### 方案一：使用 sudo 删除（推荐）

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub

# 使用 sudo 删除 .user.ini 文件
sudo rm -f dist/.user.ini

# 重新构建
npm run build
```

---

### 方案二：修改文件权限后删除

```bash
cd /www/wwwroot/feihub

# 修改文件权限
chmod 777 dist/.user.ini

# 删除文件
rm -f dist/.user.ini

# 重新构建
npm run build
```

---

### 方案三：清空 dist 目录（如果方案一不行）

```bash
cd /www/wwwroot/feihub

# 使用 sudo 清空 dist 目录
sudo rm -rf dist/*

# 重新构建
npm run build
```

---

## 🚀 快速修复（推荐方案一）

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub

# 使用 sudo 删除 .user.ini 文件
sudo rm -f dist/.user.ini

# 重新构建
npm run build
```

---

## ✅ 验证修复

构建成功后，应该看到：

```
✓ built in X.XXs
```

然后：
1. **在浏览器访问**：`http://121.40.214.130`
2. **清除浏览器缓存**（按 `Ctrl + F5`）
3. **测试提交文档**

---

## 📝 关于文件权限

`.user.ini` 文件可能是宝塔面板以特定用户创建的，需要 root 权限才能删除。

---

## 🎯 现在执行

先执行快速修复命令：

```bash
cd /www/wwwroot/feihub
sudo rm -f dist/.user.ini
npm run build
```

告诉我结果，我们继续！



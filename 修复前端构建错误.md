# 修复前端构建错误

## 问题说明

错误：`'response.data' is possibly 'undefined'`

这是因为 TypeScript 检测到 `response.data` 可能是 `undefined`，需要添加可选链操作符。

---

## 🔧 解决方案

### 方法一：从 GitHub 拉取最新代码（推荐）

如果已经推送了修复：

```bash
cd /www/wwwroot/feihub
git pull
cd ..
npm run build
```

---

### 方法二：在服务器上直接修复

在宝塔文件管理器中：
1. 进入 `/www/wwwroot/feihub/src/store`
2. 编辑 `useDocumentStore.ts`
3. 找到第 98 行：
   ```typescript
   const completedDoc = updatedDocs.find(doc => doc.id === response.data.documentId);
   ```
4. 修改为：
   ```typescript
   const completedDoc = updatedDocs.find(doc => doc.id === response.data?.documentId);
   ```

**或者使用终端命令快速修复**：

```bash
cd /www/wwwroot/feihub
sed -i 's/response.data.documentId/response.data?.documentId/' src/store/useDocumentStore.ts
npm run build
```

---

## 🚀 快速修复命令

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub

# 修复类型错误
sed -i 's/response.data.documentId/response.data?.documentId/' src/store/useDocumentStore.ts

# 重新构建
npm run build
```

---

## ✅ 成功标志

构建成功后，应该看到：
- 生成 `dist/` 目录
- `dist/` 目录中有 `index.html` 和其他静态文件
- 没有错误信息

---

## 🎯 现在执行

先执行快速修复命令：

```bash
cd /www/wwwroot/feihub
sed -i 's/response.data.documentId/response.data?.documentId/' src/store/useDocumentStore.ts
npm run build
```

告诉我结果，我们继续。



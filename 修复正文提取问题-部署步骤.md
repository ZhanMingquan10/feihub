# 修复正文提取问题 - 部署步骤

## 🔍 问题分析

正文显示 "Help CenterKeyboard Shortcuts" 而不是实际内容，说明提取到了导航栏或帮助中心的内容。

## ✅ 已修复

改进了 `backend/src/lib/feishu-server.ts` 中的内容提取逻辑：
1. 添加了更多排除规则（排除 help、shortcut、导航等元素）
2. 添加了文本过滤（排除包含 "Help Center"、"Keyboard Shortcuts" 的内容）
3. 改进了正文识别逻辑（优先选择包含中文或较长段落的内容）

## 🚀 部署步骤

### 方法一：提交到 GitHub 后部署（推荐）

**本地执行：**
```bash
git add backend/src/lib/feishu-server.ts
git commit -m "修复正文提取：排除导航栏和帮助中心内容"
git push origin main
```

**服务器执行：**
```bash
cd /www/wwwroot/feihub

# 拉取最新代码
git pull origin main

# 重新构建后端
cd backend
npm run build

# 重启后端服务
pm2 restart feihub-backend
```

### 方法二：直接在服务器上修改（更快）

在服务器上执行：

```bash
cd /www/wwwroot/feihub/backend/src/lib

# 备份原文件
cp feishu-server.ts feishu-server.ts.bak

# 使用 Python 脚本修改（我会提供具体命令）
# 或者使用 nano 手动编辑
nano feishu-server.ts
```

然后找到内容提取的部分（大约第155-190行），按照修复后的代码修改。

## 📝 快速部署命令（服务器）

```bash
cd /www/wwwroot/feihub && \
git pull origin main && \
cd backend && \
npm run build && \
pm2 restart feihub-backend && \
echo "✅ 部署完成！"
```

## ✅ 验证

1. 重新提交之前失败的链接：`https://ai.feishu.cn/docx/VGoXdFXmooasHUxsZ0icAD2WnGe`
2. 检查正文是否显示正确："嗨，你好，我是安七。应届毕业生，1年双百万，主攻ai+写作。"
3. 确认不再显示 "Help CenterKeyboard Shortcuts"

---

## 🎯 推荐使用方法一（提交到 GitHub）

请先在本地提交代码，然后在服务器上执行部署命令。


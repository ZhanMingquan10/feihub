# 检查服务器代码结构

## 🔍 第一步：检查服务器上的实际文件

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub/backend/src/lib

# 查看所有文件
ls -la

# 检查 feishu.ts 是否存在
cat feishu.ts | head -50

# 检查 feishu-puppeteer.ts 是否存在
cat feishu-puppeteer.ts | head -50
```

---

## 📋 第二步：查看实际使用的代码

```bash
# 查看 documentProcessor.ts 引用的文件
cd /www/wwwroot/feihub/backend/src/services
cat documentProcessor.ts | grep -E "(import|from)" | head -10

# 查看 feishu.ts 的内容（如果存在）
cd /www/wwwroot/feihub/backend/src/lib
cat feishu.ts | head -100
```

---

## 🚀 第三步：如果文件存在，拉取到本地

```bash
# 在本地执行（需要先配置 SSH）
git pull origin main
```

---

请先执行第一步和第二步，把结果发给我，我就能知道服务器上实际使用的代码结构。


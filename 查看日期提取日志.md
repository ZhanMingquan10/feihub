# 查看日期提取日志

## 🔍 查看详细的日期提取日志

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub/backend

# 查看最近的文档处理日志（包含日期提取信息）
pm2 logs feihub-backend --lines 200 --nostream | grep -E "(Puppeteer|日期|date|提取|处理文档)" | tail -50

# 或者查看所有输出日志
pm2 logs feihub-backend --lines 500 --nostream | tail -100
```

---

## 📝 重点关注

请把包含以下关键词的日志发给我：
- `[Puppeteer] 提取的日期`
- `[Puppeteer] 最终提取的日期`
- `[处理文档] - 日期`
- `[Puppeteer] 开始提取文档信息`
- `[Puppeteer] doc-info-time-item`

---

## 🔧 如果 Puppeteer 失败

如果日志显示 Puppeteer 失败并回退到 cheerio，可能是：
1. Chrome 未正确安装
2. Puppeteer 启动失败
3. 页面加载超时

请检查：
```bash
# 检查 Chrome
which google-chrome-stable || which chromium-browser || which chromium

# 检查 Puppeteer 启动日志
pm2 logs feihub-backend --lines 500 --nostream | grep -E "(Puppeteer|Chrome|启动)" | tail -30
```



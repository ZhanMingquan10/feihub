# 查看完整启动日志

## 🔍 查看所有启动日志（不筛选）

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub/backend

# 查看所有启动日志（最近100行，不筛选）
pm2 logs feihub-backend --lines 100 --nostream | tail -50

# 或者查看所有包含"启动"的日志
pm2 logs feihub-backend --lines 200 --nostream | grep "启动"
```

---

## 🔧 如果还是没有新的日志

可能需要：
1. **确认代码已保存**：检查 `src/index.ts` 是否包含新的日志
2. **重新构建**：确保构建成功
3. **清除 PM2 日志**：`pm2 flush` 然后重启

---

## 📝 或者直接测试环境变量

```bash
cd /www/wwwroot/feihub/backend

# 测试环境变量加载（使用正确的路径）
node -e "
const path = require('path');
const dotenv = require('dotenv');
const envPath = path.resolve(__dirname, '..', '.env');
console.log('尝试加载 .env 文件:', envPath);
dotenv.config({ path: envPath });
console.log('CHROME_PATH:', process.env.CHROME_PATH);
"
```



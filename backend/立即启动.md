# ⚡ 立即启动 - 3步搞定

## 第一步：创建 .env 文件

在 `backend` 目录下创建 `.env` 文件（新建文本文件，重命名为 `.env`），复制以下内容：

```env
PORT=4000
NODE_ENV=development
DATABASE_URL="postgresql://feihub:feihub_password@localhost:5432/feihub?schema=public"
REDIS_URL="redis://localhost:6379"
DEEPSEEK_API_KEY=sk-dff2ea5fca7c4829a3c840b2d597ebbb
OPENAI_API_KEY=
CORS_ORIGIN=http://localhost:5173
```

## 第二步：启动 Docker（如果已安装）

打开命令行，进入 `backend` 目录，执行：

```bash
docker-compose up -d
```

**如果没有 Docker：**
- Windows: 下载 https://www.docker.com/products/docker-desktop
- 安装后重启电脑，再执行上面的命令

## 第三步：启动后端

### Windows 用户：
双击运行 `start.bat` 文件

### 或手动执行：

```bash
cd backend
npm install
npm run db:generate
npm run db:migrate
npm run dev
```

## ✅ 验证启动成功

打开浏览器访问：http://localhost:4000/health

看到以下内容说明成功：
```json
{"status":"ok","timestamp":"2025-01-XX..."}
```

## 🎉 完成！

现在可以：
1. 在前端页面提交飞书文档链接
2. 系统会自动调用 DeepSeek API 生成标签和摘要
3. 数据保存到 PostgreSQL 数据库

---

## 如果遇到问题

### Docker 启动失败
- 确保 Docker Desktop 正在运行
- 检查端口 5432 和 6379 是否被占用

### npm install 失败
```bash
npm cache clean --force
npm install
```

### 数据库连接失败
```bash
# 检查 Docker 容器
docker ps

# 如果容器未运行，重新启动
docker-compose up -d
```

### 查看日志
后端启动后，控制台会显示：
- `🚀 Server running on http://localhost:4000`
- `使用 DeepSeek API，模型: deepseek-chat`（提交文档时）




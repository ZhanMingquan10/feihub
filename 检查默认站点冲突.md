# 检查默认站点冲突

## 🔍 问题分析

从测试结果看：
- 配置正确 ✓
- 通过 Host 头访问正常 ✓
- 但通过 IP 访问还是显示"没有找到站点"

**可能原因**：有其他站点配置了 `default_server` 或 `_`，覆盖了我们的配置。

---

## 🔧 解决方案

### 第一步：检查所有站点配置

在宝塔终端执行：

```bash
# 1. 查看所有站点配置文件
ls -la /www/server/panel/vhost/nginx/*.conf

# 2. 检查哪些站点配置了 default_server 或 _
grep -l "default_server\|server_name.*_" /www/server/panel/vhost/nginx/*.conf

# 3. 查看默认站点的配置
grep -A 5 "default_server" /www/server/panel/vhost/nginx/*.conf
```

---

### 第二步：检查默认站点

在宝塔面板：
1. 点击左侧 **"网站"**
2. 查看站点列表
3. 检查是否有其他站点（特别是默认站点或空站点）
4. 如果有，可以：
   - **临时禁用**（点击站点右侧的开关）
   - 或者 **删除**（如果不需要）

---

### 第三步：修改 feihub.top 为默认站点

在宝塔文件管理器中：
1. 进入 `/www/server/panel/vhost/nginx/`
2. 编辑 `feihub.top.conf`
3. 找到 `listen 80;` 行
4. 修改为：
   ```nginx
   listen 80 default_server;
   ```
5. 保存文件
6. 在宝塔面板重载 Nginx

**或者使用终端命令**：

```bash
# 修改为默认站点
sed -i 's/listen 80;/listen 80 default_server;/' /www/server/panel/vhost/nginx/feihub.top.conf

# 检查配置
nginx -t

# 重载 Nginx
nginx -s reload
```

---

## 🚀 快速修复（推荐）

在宝塔终端执行：

```bash
# 1. 检查哪些站点是默认的
grep -l "default_server\|server_name.*_" /www/server/panel/vhost/nginx/*.conf

# 2. 将 feihub.top 设置为默认站点
sed -i 's/listen 80;/listen 80 default_server;/' /www/server/panel/vhost/nginx/feihub.top.conf

# 3. 检查配置
nginx -t

# 4. 重载 Nginx
nginx -s reload

# 5. 测试访问
curl -I http://localhost
```

---

## ✅ 验证修复

修复后，测试：

1. **在浏览器访问**：`http://你的服务器IP`
   - 应该能看到 FeiHub 网站首页

2. **测试本地访问**：
   ```bash
   curl http://localhost | head -20
   ```
   - 应该返回 FeiHub 的 HTML

---

## 📝 说明

- `default_server` 表示这是默认站点，匹配所有未匹配的请求
- 设置 `default_server` 后，通过 IP 访问会优先匹配这个站点
- 如果有多个 `default_server`，Nginx 会使用第一个

---

## 🎯 现在执行

先执行快速修复命令：

```bash
grep -l "default_server\|server_name.*_" /www/server/panel/vhost/nginx/*.conf
sed -i 's/listen 80;/listen 80 default_server;/' /www/server/panel/vhost/nginx/feihub.top.conf
nginx -t
nginx -s reload
```

然后：
1. **在浏览器测试**：`http://你的服务器IP`
2. **把结果告诉我**



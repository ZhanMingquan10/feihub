# 优化正文显示为10行 - 部署步骤

## ✅ 代码已修改

前端代码已修改，正文现在会统一显示10行。

---

## 🚀 部署步骤

### 1. 在本地构建前端

```bash
cd "D:\AI+CPS(只做一件事_做好一件事)\13.出海工具站\feihub"

# 构建前端
npm run build
```

---

### 2. 推送到 GitHub

```bash
# 添加更改
git add .

# 提交
git commit -m "优化正文显示：统一显示10行"

# 推送
git push origin main
```

---

### 3. 在服务器上拉取并部署

```bash
cd /www/wwwroot/feihub

# 拉取最新代码
git pull origin main

# 进入前端目录
cd dist

# 备份旧的 dist（如果需要）
# 然后从新的构建中复制文件

# 或者直接重新构建前端（如果服务器上有 Node.js）
cd ..
npm run build
```

---

## 📝 修改说明

- 移除了字符数限制（之前是500字符）
- 添加了 `line-clamp-10` CSS 类，限制显示10行
- 使用 `-webkit-line-clamp` 确保兼容性

---

修改已完成，请按照步骤部署。


# 查看日期提取代码位置

## 🔍 第一步：找到日期提取的代码

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub/backend/src/lib

# 查找日期提取相关的代码
grep -n "提取的日期\|提取日期\|date" feishu-puppeteer.ts | head -20

# 查看日期提取函数的位置（通常在提取标题和作者之后）
grep -n "extractDate\|getDate\|日期" feishu-puppeteer.ts | head -30
```

---

## 📋 第二步：查看日期提取的具体代码

找到日期提取的代码行号后，查看那部分代码：

```bash
# 假设日期提取在 200-250 行（需要根据第一步的结果调整）
sed -n '200,250p' feishu-puppeteer.ts

# 或者查看整个文件，找到日期相关的部分
grep -B 5 -A 20 "提取的日期" feishu-puppeteer.ts
```

---

请先执行第一步，把结果发给我，特别是：
- `grep -n "提取的日期" feishu-puppeteer.ts` 的输出
- `grep -B 5 -A 20 "提取的日期" feishu-puppeteer.ts` 的输出

这样我就能知道日期提取代码的确切位置，然后直接修复。


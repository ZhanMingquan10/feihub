# 查看完整日期提取逻辑

## 🔍 查看 pageData 提取和日期查找的完整代码

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub/backend/src/lib

# 查看 pageData 提取的代码（通常在 150-280 行）
sed -n '150,280p' feishu-puppeteer.ts

# 查看日期元素查找的代码（221-250行）
sed -n '221,250p' feishu-puppeteer.ts

# 查看 parseChineseDate 函数
grep -n "parseChineseDate\|function parseChineseDate" feishu-puppeteer.ts
grep -A 30 "parseChineseDate" feishu-puppeteer.ts | head -40
```

---

请把这三个命令的输出发给我，特别是：
1. `sed -n '150,280p' feishu-puppeteer.ts` - 查看 pageData 提取逻辑
2. `sed -n '221,250p' feishu-puppeteer.ts` - 查看日期元素查找逻辑
3. `grep -A 30 "parseChineseDate"` - 查看日期解析函数

这样我就能知道问题出在哪里，然后直接修复。


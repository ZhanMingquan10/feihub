# 测试爬取内容 - 查看实际提取的数据

## 🚀 在服务器上执行测试

在服务器上执行以下命令，查看实际爬取到的内容：

```bash
cd /www/wwwroot/feihub/backend

# 创建测试脚本
cat > test_extract.js << 'JSEOF'
const { fetchFeishuDocument } = require('./dist/lib/feishu');

const link = 'https://ai.feishu.cn/docx/VGoXdFXmooasHUxsZ0icAD2WnGe';

console.log('=== 开始测试爬取 ===');
console.log('链接:', link);
console.log('');

fetchFeishuDocument(link)
  .then(result => {
    console.log('=== 爬取结果 ===');
    console.log('');
    console.log('标题 (title):');
    console.log(result.title);
    console.log('');
    console.log('作者 (author):');
    console.log(result.author);
    console.log('');
    console.log('日期 (date):');
    console.log(result.date);
    console.log('');
    console.log('内容长度 (content.length):');
    console.log(result.content.length);
    console.log('');
    console.log('内容前500字符:');
    console.log(result.content.substring(0, 500));
    console.log('');
    console.log('内容完整内容:');
    console.log('---开始---');
    console.log(result.content);
    console.log('---结束---');
    process.exit(0);
  })
  .catch(error => {
    console.error('=== 爬取失败 ===');
    console.error('错误:', error.message);
    console.error('堆栈:', error.stack);
    process.exit(1);
  });
JSEOF

# 执行测试
node test_extract.js

# 清理
rm -f test_extract.js
```

---

## 📝 或者查看最近的爬取日志

```bash
cd /www/wwwroot/feihub/backend

# 查看最近爬取这个链接的日志
pm2 logs feihub-backend --lines 1000 --nostream | grep -A 50 "VGoXdFXmooasHUxsZ0icAD2WnGe" | tail -100
```

---

## 📝 或者直接查看数据库中的内容

```bash
cd /www/wwwroot/feihub/backend

# 使用 Prisma Studio 查看（如果有）
# npx prisma studio

# 或者直接查询数据库
# 需要先找到这个文档的 ID 或链接
```

---

推荐使用第一个方法（测试脚本），可以实时查看爬取结果。


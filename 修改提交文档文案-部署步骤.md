# 修改提交文档文案 - 部署步骤

## ✅ 已修改的文件

1. `src/store/useDocumentStore.ts` - 前端默认文案
2. `backend/src/routes/submissions.ts` - 后端返回的文案

## 🚀 部署步骤

### 第一步：提交代码到 GitHub（本地）

在本地执行：

```bash
git add src/store/useDocumentStore.ts backend/src/routes/submissions.ts
git commit -m "修改提交文档文案：AI正在处理中，预计需要几分钟"
git push origin main
```

### 第二步：在服务器上拉取并部署

在服务器上执行：

```bash
cd /www/wwwroot/feihub

# 拉取最新代码
git pull origin main

# 重新构建前端
npm run build

# 重新构建后端
cd backend
npm run build

# 重启后端服务
pm2 restart feihub-backend

# 重载 Nginx
nginx -s reload
```

### 第三步：验证

1. 访问网站
2. 点击"分享文档"
3. 提交一个文档链接
4. 查看弹窗文案是否显示："文档已提交，AI正在处理中，预计需要几分钟..."

---

## 📝 快速部署命令（服务器）

```bash
cd /www/wwwroot/feihub && \
git pull origin main && \
npm run build && \
cd backend && \
npm run build && \
pm2 restart feihub-backend && \
cd .. && \
nginx -s reload && \
echo "✅ 部署完成！"
```

---

请先在本地提交代码，然后在服务器上执行部署命令。


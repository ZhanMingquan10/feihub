# 检查并修复网站配置

## 📋 配置文件分析

从配置文件看，有几个需要注意的地方：

1. **反向代理配置**在：`/www/server/panel/vhost/nginx/proxy/feihub.top/*.conf`
2. **PHP 配置**：`include enable-php-00.conf;`（静态网站不需要，但保留也不影响）
3. **根目录**：`/www/wwwroot/feihub/dist` ✓ 正确

---

## 🔍 第一步：检查反向代理配置文件

在宝塔终端执行：

```bash
# 查看反向代理配置文件
ls -la /www/server/panel/vhost/nginx/proxy/feihub.top/

# 查看反向代理配置内容
cat /www/server/panel/vhost/nginx/proxy/feihub.top/*.conf
```

**把结果发给我，我帮你检查。**

---

## 🔍 第二步：测试本地访问

```bash
# 测试本地访问
curl -I http://localhost

# 测试直接访问文件
curl http://localhost/index.html
```

---

## 🔧 可能的问题和解决方案

### 问题 1：反向代理配置覆盖了静态文件

如果反向代理配置是 `location /` 而不是 `location /api`，会覆盖静态文件。

**解决方案**：检查并修复反向代理配置文件。

---

### 问题 2：域名解析未生效

**检查方法**：
- 在本地电脑执行 `ping feihub.top`
- 查看返回的 IP 是否是服务器 IP

**解决方案**：
- 如果 IP 不对，检查域名 DNS 配置
- 等待 DNS 生效

---

### 问题 3：防火墙阻止访问

**检查方法**：
- 在宝塔面板 → 安全 → 防火墙
- 检查 80 和 443 端口是否开放

**解决方案**：
- 如果未开放，添加规则开放 80 和 443 端口

---

## 🚀 快速测试

在宝塔终端执行：

```bash
# 1. 查看反向代理配置
cat /www/server/panel/vhost/nginx/proxy/feihub.top/*.conf

# 2. 测试本地访问
curl -I http://localhost

# 3. 测试直接访问文件
curl http://localhost/index.html | head -20
```

**把结果发给我，特别是反向代理配置文件的内容。**

---

## 🎯 现在执行

先执行这些命令：

```bash
# 查看反向代理配置文件
cat /www/server/panel/vhost/nginx/proxy/feihub.top/*.conf

# 测试本地访问
curl -I http://localhost
```

这样我就能知道具体是什么问题了。



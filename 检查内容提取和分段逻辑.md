# 检查内容提取和分段逻辑

## 🔍 在服务器上执行

在宝塔终端执行：

```bash
cd /www/wwwroot/feihub/backend/src/lib

# 查找内容提取相关的代码
grep -n "从页面直接提取内容\|内容预览\|最终内容长度" feishu-puppeteer.ts

# 查看内容提取的完整代码（通常在 400-500 行）
grep -A 50 "从页面直接提取内容" feishu-puppeteer.ts | head -60
```

---

## 🔧 需要添加的功能

在内容提取后，需要添加智能分段逻辑：
1. 保留段落分隔（`<p>`, `div`, `br`, `h1-h6`, `li` 等）
2. 在句号、问号、感叹号后添加换行
3. 清理多余的空行

---

请先执行第一步，把内容提取的代码发给我，我会添加智能分段功能。

